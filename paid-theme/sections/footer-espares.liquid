<style>
:root {
  --primary-orange: #ff6b35;
  --primary-orange-light: #ff8a5e;
  --primary-orange-dark: #e55a2b;
  --accent-orange: #ffab47;
  --pure-white: #ffffff;
  --light-gray: #f8f9fa;
  --border-gray: #e9ecef;
  --text-dark: #2c3e50;
  --text-muted: #6c757d;
  --shadow-light: 0 4px 12px rgba(255, 107, 53, 0.08);
  --shadow-medium: 0 6px 20px rgba(255, 107, 53, 0.12);
  --shadow-heavy: 0 10px 30px rgba(255, 107, 53, 0.15);
  --border-radius: 16px;
  --border-radius-small: 8px;
  --transition: all 0.4s cubic-bezier(0.4, 0.0, 0.2, 1);
}

/* PartsFix Modern Footer */
.parts-footer-wrap {
  background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
  color: var(--pure-white);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  position: relative;
  overflow: hidden;
}

.parts-footer-wrap::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6px;
  background: linear-gradient(90deg, var(--primary-orange), var(--accent-orange), var(--primary-orange));
  animation: footerTopFlow 8s ease-in-out infinite;
}

@keyframes footerTopFlow {
  0%, 100% { transform: translateX(-100px); }
  50% { transform: translateX(100px); }
}

.parts-footer-wrap::after {
  content: '';
  position: absolute;
  top: 20%;
  right: -10%;
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, rgba(255, 107, 53, 0.1) 0%, transparent 70%);
  border-radius: 50%;
  animation: floatOrb 12s ease-in-out infinite;
}

@keyframes floatOrb {
  0%, 100% { transform: translate(0, 0) scale(1); }
  33% { transform: translate(-50px, -30px) scale(1.1); }
  66% { transform: translate(30px, -60px) scale(0.9); }
}

.parts-footer {
  padding: 80px 0 40px;
  position: relative;
  z-index: 2;
}

.parts-footer-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 24px;
}

.parts-footer-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1.2fr;
  gap: 60px;
  margin-bottom: 60px;
}

.parts-footer-column {
  position: relative;
}

.parts-footer-heading {
  font-weight: 800;
  font-size: 1.4rem;
  color: var(--pure-white);
  margin: 0 0 32px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  position: relative;
  padding-bottom: 16px;
}

.parts-footer-heading::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 60px;
  height: 4px;
  background: linear-gradient(90deg, var(--primary-orange), var(--accent-orange));
  border-radius: 2px;
}

/* Contact Info Styling */
.parts-footer-contact {
  line-height: 1.8;
}

.parts-footer-contact p {
  margin: 0 0 20px;
  color: #bdc3c7;
  font-size: 1rem;
  font-weight: 400;
}

.parts-footer-contact strong {
  color: var(--pure-white);
  font-weight: 600;
}

.parts-contact-item {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--border-radius);
  padding: 24px;
  margin-bottom: 20px;
  backdrop-filter: blur(10px);
  transition: var(--transition);
}

.parts-contact-item:hover {
  background: rgba(255, 107, 53, 0.1);
  border-color: rgba(255, 107, 53, 0.3);
  transform: translateY(-4px);
  box-shadow: var(--shadow-medium);
}

.parts-contact-item h4 {
  font-weight: 700;
  font-size: 1.1rem;
  color: var(--primary-orange);
  margin: 0 0 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.parts-contact-item h4::before {
  content: '';
  width: 8px;
  height: 8px;
  background: var(--accent-orange);
  border-radius: 50%;
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.2); opacity: 0.7; }
}

.parts-contact-item p {
  margin: 0;
  color: #ecf0f1;
  font-size: 0.95rem;
}

/* Footer Links */
.parts-footer-links {
  list-style: none;
  margin: 0;
  padding: 0;
}

.parts-footer-links li {
  margin: 0 0 16px;
  transform: translateX(0);
  transition: var(--transition);
}

.parts-footer-links li:hover {
  transform: translateX(8px);
}

.parts-footer-links a {
  color: #bdc3c7;
  text-decoration: none;
  font-size: 1rem;
  font-weight: 500;
  transition: var(--transition);
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 0;
  border-radius: var(--border-radius-small);
}

.parts-footer-links a::before {
  content: '→';
  font-size: 0.9rem;
  color: var(--primary-orange);
  transition: var(--transition);
  opacity: 0;
  transform: translateX(-10px);
}

.parts-footer-links a:hover {
  color: var(--pure-white);
  background: rgba(255, 107, 53, 0.1);
  padding-left: 16px;
}

.parts-footer-links a:hover::before {
  opacity: 1;
  transform: translateX(0);
}

/* Newsletter Section */
.parts-newsletter-section {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 24px;
  padding: 32px;
  backdrop-filter: blur(10px);
  position: relative;
  overflow: hidden;
}

.parts-newsletter-section::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(255, 107, 53, 0.05) 0%, transparent 70%);
  animation: rotate 20s linear infinite;
}

@keyframes rotate {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.parts-newsletter-section > * {
  position: relative;
  z-index: 2;
}

.parts-newsletter-desc {
  color: #bdc3c7;
  margin: 0 0 24px;
  line-height: 1.6;
  font-size: 1rem;
}

.parts-newsletter-form {
  position: relative;
}

.parts-newsletter-input-wrapper {
  position: relative;
  display: flex;
  margin-bottom: 16px;
}

.parts-newsletter-input {
  flex: 1;
  padding: 16px 60px 16px 20px;
  border: 2px solid rgba(255, 255, 255, 0.2);
  border-radius: var(--border-radius);
  font-size: 1rem;
  background: rgba(255, 255, 255, 0.1);
  color: var(--pure-white);
  transition: var(--transition);
  backdrop-filter: blur(10px);
}

.parts-newsletter-input::placeholder {
  color: #bdc3c7;
}

.parts-newsletter-input:focus {
  outline: none;
  border-color: var(--primary-orange);
  background: rgba(255, 255, 255, 0.15);
  box-shadow: 0 0 0 4px rgba(255, 107, 53, 0.2);
}

.parts-newsletter-submit {
  position: absolute;
  right: 8px;
  top: 50%;
  transform: translateY(-50%);
  background: linear-gradient(135deg, var(--primary-orange), var(--accent-orange));
  color: var(--pure-white);
  border: none;
  padding: 12px;
  border-radius: var(--border-radius-small);
  cursor: pointer;
  transition: var(--transition);
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
}

.parts-newsletter-submit:hover {
  transform: translateY(-50%) scale(1.1);
  box-shadow: var(--shadow-medium);
}

.parts-newsletter-submit svg {
  width: 20px;
  height: 20px;
}

/* Sub Footer */
.parts-sub-footer {
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  padding-top: 40px;
  text-align: center;
  position: relative;
}

.parts-sub-footer::before {
  content: '';
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 200px;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--primary-orange), transparent);
}

.parts-sub-footer p {
  color: #95a5a6;
  font-size: 0.95rem;
  margin: 0;
  font-weight: 400;
}

.parts-sub-footer strong {
  color: var(--primary-orange);
  font-weight: 700;
}

/* Responsive Design */
@media (max-width: 1024px) {
  .parts-footer-grid {
    grid-template-columns: 1fr 1fr;
    gap: 40px;
  }
  
  .parts-footer {
    padding: 60px 0 40px;
  }
}

@media (max-width: 768px) {
  .parts-footer-container {
    padding: 0 16px;
  }
  
  .parts-footer-grid {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  
  .parts-footer {
    padding: 50px 0 30px;
  }
  
  .parts-footer-heading {
    font-size: 1.2rem;
    margin-bottom: 24px;
  }
  
  .parts-contact-item {
    padding: 20px;
  }
  
  .parts-newsletter-section {
    padding: 24px;
  }
  
  .parts-newsletter-input {
    padding: 14px 50px 14px 16px;
  }
  
  .parts-newsletter-submit {
    width: 40px;
    height: 40px;
  }
}

@media (max-width: 480px) {
  .parts-footer-heading {
    font-size: 1.1rem;
  }
  
  .parts-footer-links a {
    font-size: 0.95rem;
  }
  
  .parts-newsletter-desc {
    font-size: 0.95rem;
  }
  
  .parts-sub-footer p {
    font-size: 0.9rem;
  }
}

/* Modal styles remain the same but with PartsFix theme */
.pf-modal-backdrop{position:fixed;inset:0;background:rgba(44, 62, 80, 0.8);display:none;z-index:9997;backdrop-filter:blur(4px)}
.pf-modal{position:fixed;inset:40px 20px 40px 20px;max-width:960px;margin:auto;background:var(--pure-white);color:var(--text-dark);border-radius:24px;box-shadow:var(--shadow-heavy);display:none;z-index:9998;overflow:auto;border:2px solid var(--primary-orange)}
.pf-modal.open{display:block}
.pf-modal-backdrop.open{display:block}
.pf-modal header{position:sticky;top:0;background:linear-gradient(135deg, var(--primary-orange), var(--accent-orange));color:var(--pure-white);border-bottom:none;padding:20px 24px;display:flex;justify-content:space-between;align-items:center;border-radius:22px 22px 0 0}
.pf-modal header h3{margin:0;font-size:1.3rem;font-weight:700}
.pf-modal header button{background:rgba(255,255,255,0.2);color:var(--pure-white);border:none;border-radius:var(--border-radius-small);padding:8px 12px;cursor:pointer;font-weight:600;transition:var(--transition)}
.pf-modal header button:hover{background:rgba(255,255,255,0.3);transform:scale(1.05)}
.pf-modal .pf-body{padding:32px 24px;line-height:1.65;font-size:1rem}
.pf-modal .pf-body h4{margin:24px 0 12px;font-size:1.2rem;color:var(--primary-orange);font-weight:700}
.pf-modal .pf-body ul{padding-left:24px;margin:12px 0}
.pf-modal .pf-body li{margin:8px 0}
.pf-modal .pf-body a{color:var(--primary-orange);text-decoration:none;font-weight:600}
.pf-modal .pf-body a:hover{color:var(--primary-orange-dark);text-decoration:underline}
body.pf-modal-open{overflow:hidden}
</style>

<div class="parts-footer-wrap">
  <div class="parts-footer">
    <div class="parts-footer-container">
      <div class="parts-footer-grid">
        <!-- Where To Find Us Column -->
        <div class="parts-footer-column">
          <h3 class="parts-footer-heading">Where To Find Us</h3>
          <div class="parts-footer-contact">
            <p><strong>PartsFix Headquarters</strong><br>
            Unit 2, Broughton Trade Centre,<br>
            Broughton Ln, Salford, M7 1UH<br>
            United Kingdom</p>
            
            <div class="parts-contact-item">
              <h4>Chat Support</h4>
              <p>Mon - Fri 9.00am - 5.00pm</p>
            </div>
            
            <div class="parts-contact-item">
              <h4>Email Support</h4>
              <p>wecare@essentialsfix.co.uk</p>
            </div>
          </div>
        </div>

        <!-- Service For All Column -->
        <div class="parts-footer-column">
          <h3 class="parts-footer-heading">Service For All</h3>
          <ul class="parts-footer-links">
            <li><a href="/pages/track-order" title="Track Order">Track Order</a></li>
            <li><a href="#" data-pf-modal="about" title="About Us">About Us</a></li>
            <li><a href="/pages/trade-application" title="Trade Application">Trade Application</a></li>
            <li><a href="/pages/returns" title="Returns & Refunds">Returns & Refunds</a></li>
            <li><a href="/pages/contact" title="Contact Us">Contact Us</a></li>
            <li><a href="/pages/supplier" title="Become A Supplier">Become A Supplier</a></li>
            <li><a href="/pages/faqs" title="FAQs">FAQs</a></li>
            <li><a href="/blogs/news" title="Blogs">Blogs</a></li>
          </ul>
        </div>

        <!-- Schemes For All Column -->
        <div class="parts-footer-column">
          <h3 class="parts-footer-heading">Policies & Info</h3>
          <ul class="parts-footer-links">
            <li><a href="/pages/privacy" title="Privacy Policy">Privacy Policy</a></li>
            <li><a href="/pages/terms" title="Terms Of Service">Terms Of Service</a></li>
            <li><a href="/pages/shipping" title="Shipping Policy">Shipping Policy</a></li>
          </ul>
        </div>

        <!-- Subscribe For All Column -->
        <div class="parts-footer-column">
          <h3 class="parts-footer-heading">Stay Connected</h3>
          <div class="parts-newsletter-section">
            <p class="parts-newsletter-desc">Join thousands of smart homeowners getting exclusive offers, expert tips, and early access to new parts.</p>
            
            <form class="parts-newsletter-form" action="{{ routes.cart_add_url }}" method="post" enctype="multipart/form-data">
              <div class="parts-newsletter-input-wrapper">
                <input type="email" 
                       name="contact[email]" 
                       class="parts-newsletter-input" 
                       placeholder="Enter your email address"
                       required
                       aria-label="Email address">
                <button type="submit" class="parts-newsletter-submit" aria-label="Subscribe">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
                  </svg>
                </button>
              </div>
              <input type="hidden" name="contact[tags]" value="newsletter">
            </form>
          </div>
        </div>
      </div>

      <div class="parts-sub-footer">
        <p>&copy; {{ 'now' | date: '%Y' }} <strong>PartsFix</strong> - All rights reserved. Your trusted partner for genuine spare parts and accessories.</p>
      </div>
    </div>
  </div>
</div>

  <!-- Policy Modals (inline, no admin pages required) -->
  <div class="pf-modal-backdrop" id="pf-modal-backdrop" aria-hidden="true"></div>

  <section class="pf-modal" id="pf-modal-privacy" role="dialog" aria-modal="true" aria-labelledby="pf-privacy-title">
    <header>
      <h3 id="pf-privacy-title">Privacy Policy</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <p>We value your privacy. This summary explains what data we collect, how we use it, and your choices.</p>
      <h4>What we collect</h4>
      <ul>
        <li>Contact details (name, email, phone)</li>
        <li>Order and delivery information</li>
        <li>Device and usage data (cookies, analytics)</li>
      </ul>
      <h4>How we use your data</h4>
      <p>To fulfil orders, provide support, improve our services, and ensure security/compliance.</p>
      <h4>Your rights</h4>
      <p>Access, correction, deletion, and restriction. Contact customer support to exercise your rights.</p>
    </div>
    </section>

  <section class="pf-modal" id="pf-modal-cookies" role="dialog" aria-modal="true" aria-labelledby="pf-cookies-title">
    <header>
      <h3 id="pf-cookies-title">Cookie Policy</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <p>Cookies help the site function and improve your experience.</p>
      <h4>Types we use</h4>
      <ul>
        <li>Essential – required for basic features</li>
        <li>Analytics – measure performance</li>
        <li>Marketing – show relevant content</li>
      </ul>
      <p>You can control cookies in your browser settings.</p>
    </div>
  </section>

  <!-- About Us Modal -->
  <section class="pf-modal" id="pf-modal-about" role="dialog" aria-modal="true" aria-labelledby="pf-about-title">
    <header>
      <h3 id="pf-about-title">About Us</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <p>We're your trusted source for spare parts and accessories. Our mission is to make fixing easy with expert advice, quality parts, and fast delivery.</p>
      <p>Questions? Use the Contact page to reach our support team.</p>
    </div>
  </section>

  <section class="pf-modal" id="pf-modal-terms" role="dialog" aria-modal="true" aria-labelledby="pf-terms-title">
    <header>
      <h3 id="pf-terms-title">Terms & Conditions</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <h4>Agreement to Terms</h4>
      <p>By using this site you agree to these terms and our privacy policy.</p>
      <h4>Orders & Payments</h4>
      <p>Orders are subject to acceptance and availability. Prices/promotions may change.</p>
      <h4>Liability</h4>
      <p>To the fullest extent permitted by law we disclaim warranties and liability arising from site use.</p>
    </div>
  </section>

  <section class="pf-modal" id="pf-modal-delivery" role="dialog" aria-modal="true" aria-labelledby="pf-delivery-title">
    <header>
      <h3 id="pf-delivery-title">Delivery Information</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <h4>Timeframes</h4>
      <p>Orders are processed within 24 hours. Delivery typically takes 1–3 working days.</p>
      <h4>Costs</h4>
      <p>Shipping fees are calculated at checkout based on weight and destination.</p>
      <h4>Tracking</h4>
      <p>We’ll send tracking details by email when your order is dispatched.</p>
    </div>
  </section>

  <section class="pf-modal" id="pf-modal-returns" role="dialog" aria-modal="true" aria-labelledby="pf-returns-title">
    <header>
      <h3 id="pf-returns-title">Returns & Refunds</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <h4>Return Window</h4>
      <p>Return items within 30 days, unused and in original packaging.</p>
      <h4>Process</h4>
      <p>Contact support with your order number to obtain a return authorization.</p>
      <h4>Refunds</h4>
      <p>Refunds are issued to the original payment method after inspection.</p>
    </div>
  </section>

  <section class="pf-modal" id="pf-modal-warranty" role="dialog" aria-modal="true" aria-labelledby="pf-warranty-title">
    <header>
      <h3 id="pf-warranty-title">Safety & Warranty</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <h4>Coverage</h4>
      <p>Most parts include a 12‑month warranty against manufacturing defects.</p>
      <h4>Safety</h4>
      <p>Always disconnect power and follow manufacturer guidance before repairs.</p>
    </div>
  </section>



  <!-- Finding My Model Number Modal -->
  <section class="pf-modal" id="pf-modal-model" role="dialog" aria-modal="true" aria-labelledby="pf-model-title">
    <header>
      <h3 id="pf-model-title">Finding My Model Number</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <p>Model numbers are usually found on the rating plate or sticker on your appliance. Common locations:</p>
      <ul>
        <li>Washing Machines: door frame or behind the door</li>
        <li>Fridges/Freezers: inside the fridge wall or behind lower drawers</li>
        <li>Cookers/Ovens: inside the door frame or on the back</li>
        <li>Vacuum Cleaners: underside or behind the dust bin</li>
      </ul>
    </div>
  </section>

  <!-- Sitemap Modal -->
  <section class="pf-modal" id="pf-modal-sitemap" role="dialog" aria-modal="true" aria-labelledby="pf-sitemap-title">
    <header>
      <h3 id="pf-sitemap-title">Sitemap</h3>
      <button type="button" data-pf-close>Close</button>
    </header>
    <div class="pf-body">
      <ul>
        <li><a href="{{ routes.root_url }}">Home</a></li>
        <li><a href="{{ routes.search_url }}">Search</a></li>
        <li><a href="/pages/contact">Contact</a></li>
        <li><a href="#" data-pf-modal="delivery">Delivery</a></li>
        <li><a href="#" data-pf-modal="returns">Returns</a></li>
        <li><a href="#" data-pf-modal="privacy">Privacy</a></li>
        <li><a href="#" data-pf-modal="terms">Terms</a></li>
        <li><a href="#" data-pf-modal="cookies">Cookies</a></li>
      </ul>
    </div>
  </section>

  <script>
    (function(){
      var backdrop = document.getElementById('pf-modal-backdrop');
      function openModal(key){
        var id = 'pf-modal-' + key;
        var m = document.getElementById(id); if(!m) return;
        m.classList.add('open'); backdrop.classList.add('open');
        m.setAttribute('aria-hidden','false');
        document.body.classList.add('pf-modal-open');
      }
      function closeAll(){
        document.querySelectorAll('.pf-modal.open').forEach(function(m){ m.classList.remove('open'); m.setAttribute('aria-hidden','true'); });
        backdrop.classList.remove('open');
        document.body.classList.remove('pf-modal-open');
      }
      document.addEventListener('click', function(e){
        var t = e.target.closest('[data-pf-modal]');
        if(t){ e.preventDefault(); openModal(t.getAttribute('data-pf-modal')); }
        if(e.target.hasAttribute('data-pf-close') || e.target === backdrop){ closeAll(); }
      });
      document.addEventListener('keydown', function(e){ if(e.key === 'Escape') closeAll(); });
      function openFromHash(){
        var h = (location.hash||'').replace('#','').toLowerCase();
  var allowed = ['privacy','cookies','terms','delivery','returns','warranty','about','model','sitemap'];
        if(allowed.indexOf(h) > -1){ openModal(h); }
      }
      window.addEventListener('hashchange', openFromHash);
      openFromHash();
    })();
    </script>
